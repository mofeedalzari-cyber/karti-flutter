# خطة تحويل كرتي إلى Flutter — مراحل كاملة

جرد كامل لكل صفحة ووظيفة بالمشروع الحالي (React)، مقسّمة لمراحل منطقية.
الهدف: ولا صفحة أو ميزة تفوتنا. كل مرحلة **تُختبر عندك فعلياً** قبل ننتقل
للي بعدها.

---

## ✅ المرحلة 1 — الأساس (تم)
- [x] الثيم (ألوان، خطوط، أنماط الأزرار والبطاقات)
- [x] تسجيل الدخول (`auth.tsx`)
- [x] لوحة التحكم — إحصائيات المدير (`app.index.tsx` → AdminDashboard)
- [x] القالب الرئيسي + شريط تنقل سفلي

## المرحلة 2 — المخزون الأساسي
- [x] `app.packages` — الباقات (عرض/إضافة/تعديل/حذف/تعطيل + طلب سحب + **بيع مباشر (sell_card) + مشاركة الفاتورة**)
- [x] `app.networks.index` — قائمة الشبكات (عرض/إضافة/تعديل/حذف + عدّادات)
- [x] `app.networks.$id` — مدمجة ضمن صفحة الباقات (نفس RPCs: sell_card, request_cards) بدل صفحة منفصلة
- [x] `app.cards` — رفع الكروت بالجملة (نص/ملف TXT-CSV-JSON) — استخراج PDF مؤجَّل للمرحلة 7 (PDF فقط، غير قابل للتأجيل لغير ذلك)
- [x] `app.manage-cards` — **كاملة 100%**: فلاتر + قائمة + تحديد جماعي + حذف/إرجاع ذكي + **نقل الكروت المباعة بين المناديب + حذف الكروت القديمة تلقائياً**
  - [ ] طباعة كشوفات PDF (printAssignedCards) — مؤجَّل للمرحلة 7 (PDF فقط)

## المرحلة 3 — طبقة العمليات (المندوبين والطلبات)
- [x] `app.agents` — قائمة المناديب (بحث، تفعيل/تعطيل، تغيير الشبكة، ملخص مبيعات)
  - [ ] لوحة الإحصائيات التفصيلية لكل مندوب (تفصيل شهري/حسب الباقة) — مؤجَّل، تفصيل بصري إضافي فوق الموجود
- [x] `app.requests` — طلبات سحب الكروت (تبويبات، اعتماد/رفض/تسجيل دفعة، حذف جماعي)
- [x] `app.join-requests` — طلبات الانضمام (تبويبات، قبول/رفض)
- [x] `register-agent` + `register-network` — شاشة تسجيل مشتركة
- [x] `app.agent-accounts` — **كاملة**: فلاتر شبكة/مندوب، إحصائيات، جدول مديونية/مدفوعات، جدول حسب الشبكة، جدول حسب الباقة، تسوية المديونيات (reconcile_agent_debts)
- [x] `app.index.tsx` → AgentDashboard — **تأكدنا: مختلفة فعلاً، صُححت** (كانت
      كل الأدوار تشوف لوحة المدير بالغلط). الآن `RoleDashboard` يوزّع صح:
      مدير → `DashboardScreen` (+ `AdminBreakdownsSection` الجديد: تفصيل حسب
      الباقة، أرصدة المناديب، تصفير الرصيد)، مندوب → `AgentHomeScreen` (إحصائياته
      الخاصة فقط)
  - [ ] تصدير Excel/PDF من لوحة التحكم (exportToExcel/exportToPDF) — مؤجَّل
        للمرحلة 7 (نفس حدود PDF المعروفة، غير قابل للتأجيل لغير ذلك)

## المرحلة 4 — المبيعات والعملاء
- [x] `app.sales` — جميع المبيعات (بحث، فلاتر عميل/مندوب/باقة، تعديل، حذف جماعي)
- [x] `app.payments` — تسديد ديون المناديب (settle_agent_debt)، سجل الدفعات، تعديل/حذف
  - [ ] طباعة إيصال PDF بعد التسديد (printReceiptPDF) — مؤجَّل للمرحلة 7 (PDF فقط)
- [x] `app.cabin` — كبينة بيع المندوب (agent_cabin RPC)، بيع إلزامي بربط زبون، إدارة الزبائن
- [x] `app.customers` — **كاملة بجزئيها**: عرض المندوب (رصيد، مبيعات، دفعات، شحن يدوي)
      وعرض المدير (network_customers، تسوية دين عبر مندوب)

## المرحلة 5 — الإدارة والإعدادات
- [x] `app.settings` — تعديل الملف الشخصي (الاسم/الهاتف)
  - [ ] BackupCard / AgentBackupCard / DangerZone (مسح البيانات) — مؤجَّل
        للمرحلة 8 (تعتمد على دوال TanStack Start على Render، نفس فئة
        الإشعارات والميكروتك)
- [x] `app.logs` — سجل النشاط (200 عملية، تحديد جماعي وحذف، تمييز سجلات محذوفة)
- [x] `app.password-resets` — طلبات استعادة كلمة المرور (سوبر أدمن)
- [x] `app.superadmin` — لوحة السوبر أدمن: إحصائيات + شبكات (تفعيل/تعطيل/حذف)
      + مناديب + باقات
  - [ ] NetworkDetail التفصيلي (كروت شبكة معينة + رسوم بيانية) — تفصيل إضافي مؤجَّل
  - [ ] NetworkBackupButton — مؤجَّل للمرحلة 8 (نفس سبب BackupCard)
- [x] الوظائف الإدارية: `admin-agents.functions.ts` (استُبدلت بـ RPC مباشر:
      set_agent_active/set_agent_network — كانت أصلاً مجرد أغلفة بسيطة)

## المرحلة 6 — الميكروتك
- [x] `app.mikrotiks` — **كاملة، وبدون أي اعتماد على سيرفر وسيط!** اتصال مباشر
      من الجوال عبر بروتوكول API الثنائي (`lib/services/routeros_api.dart`،
      تنفيذ كامل بـ Dart حسب توثيق MikroTik الرسمي) — يعمل مع RouterOS v6
      وv7، ومحلياً أو عبر الإنترنت. تبويبات: عامة، النشطون، الكروت، الباقات
      + إضافة/تعديل/حذف جهاز + اختبار اتصال قبل الحفظ.
  - ⚠️ **لم يُختبر ضد جهاز حقيقي من بيئتي** (لا شبكة عندي) — أول اختبار فعلي
    عندك، نفس قاعدة العمل المتبعة طول المشروع.

## المرحلة 7 — ميزات خاصة (PDF + إشعارات + جهات اتصال)
- [x] **الإشعارات (Firebase)**: `push-notifications.ts` بالكامل — نفس مشروع
      Firebase (`kary-95ae6-a5e60`)، نفس قناة `karti_requests`، نفس دالة
      RPC `register_device_token`، فتح المسار المناسب عند الضغط على الإشعار.
- [x] **جهات الاتصال**: `pick-contact.ts` — عبر `flutter_contacts` (بديل حديث
      لـ `@capacitor-community/contacts`)، مربوطة بنموذج إضافة زبون بالكابينة.
- [x] **PDF — الأساسيات**:
  - إيصال دفع (`receipt-pdf.ts`) — مربوط فعلياً بشاشة المدفوعات (بديل التصميم
    الرسمي بـ pdfmake، بنفس البيانات والمحتوى)
  - طباعة كروت (`card-print.ts`) — **نسخة مبسّطة**: تصميم قياسي نظيف بدل
    نظام "القالب المخصص" (رفع صورة خلفية + تحديد موقع الكود عليها بالبكسل)
    لأن هذا الجزء تحديداً عمل كبير منفصل بذاته. مربوط بشاشة إدارة الكروت.
  - خط Cairo عبر `PdfGoogleFonts` (حزمة `printing` الرسمية) بدل تضمين ملف
    خط أو الاعتماد على رابط ثابت
- [x] **فاتورة الزبون (`customer-invoice-pdf.ts`)**: بنود الشراء، سجل
      التسديد/الشحن، تحويل المبلغ لكلمات عربية (نفس دوال
      numberToArabicWords/currencyWord حرفياً)، رقم فاتورة تسلسلي محلي.
      مربوطة فعلياً بزر "طباعة فاتورة" بشاشة تفاصيل الزبون.
- [x] **استخراج كروت من PDF (`pdf-extract.ts`)**: نفس منطق الـ Regex الأصلي
      حرفياً لتحليل النص لأزواج مستخدم/كلمة مرور. مربوط فعلياً برفع الكروت
      (دعم .pdf بجانب .txt/.csv/.json).
      ⚠️ **تنويه ترخيص مهم**: يستخدم حزمة `syncfusion_flutter_pdf` لاستخراج
      النص من PDF — تتطلب رخصة Syncfusion (مجانية لو ربح شركتك أقل من حد
      معيّن سنوياً، أو مدفوعة غير ذلك). **تأكد من الأهلية قبل نشر هذي الميزة
      بالإنتاج** — راجع syncfusion.com/sales/communitylicense. لو ما
      تنطبق عليك الرخصة المجانية، عطّل دعم PDF واكتفِ بـ TXT/CSV/JSON
      (تشتغل بدون أي مكتبة مرخّصة).
- [x] **تقرير لوحة التحكم / تصدير Excel (`pdfmake-report.ts` + `dashboard-export.ts`)**:
      مولّد تقرير عام (ملخص + أقسام جداول) قابل لإعادة الاستخدام — تصدير PDF
      وExcel معاً، مربوط فعلياً بزرّين بلوحة تحكم المدير (حسب الباقة، أرصدة
      المناديب، المناديب). استُخدمت حزمة `excel` (رخصة MIT مفتوحة تماماً)
      للإكسل بدل أي حل يحتاج ترخيصاً تجارياً.

## ✅ المرحلة 7 مكتملة 100%
كل عناصرها التسعة الأصلية أُنجزت: الإشعارات، جهات الاتصال، إيصال الدفع،
طباعة الكروت (نسخة مبسّطة موثّقة)، فاتورة الزبون، استخراج كروت من PDF، تقرير
PDF/Excel. الاستثناء الوحيد المتبقي عمداً: نظام "القالب المخصص" لطباعة
الكروت (تخصيص صورة خلفية بالبكسل) — ميزة منفصلة بذاتها، مو أساسية للطباعة.

## المرحلة 8 — قرار السيرفر (حاسمة)
### 🎉 النتيجة: لا حاجة لأي سيرفر وسيط إطلاقاً!

فحصت كل دوال `createServerFn` المتبقية على Render وتأكدت: **ولا وحدة منها
تستخدم `SUPABASE_SERVICE_ROLE_KEY` أو أي مفتاح سري**. كلها فعلياً أغلفة حول:
- استعلامات Supabase عادية بصلاحيات المستخدم نفسه (RLS يكفي وحده للحماية)
- دوال RPC آمنة (`SECURITY DEFINER`) موجودة أصلاً بقاعدة البيانات

يعني كل شي بُني **مباشرة بـFlutter، بدون Render ولا Supabase Edge
Functions** — نفس نمط كل صفحة سابقة بالضبط:

- [x] `fcm.server.ts` + `push.functions.ts` — **لم تعد مطلوبة**: الإشعارات
      تُرسل من قاعدة البيانات مباشرة عبر Triggers/الاستدعاءات الحالية،
      والاستقبال بُني بالمرحلة 7 (Firebase مباشرة)
- [x] `mikrotik.server.ts` — **لم تعد مطلوبة**: استُبدلت بالكامل ببروتوكول
      API ثنائي مباشر من الجوال (المرحلة 6)
- [x] `admin-wipe.functions.ts` → `wipeAllData()` — RPC مباشر `admin_wipe_database`
- [x] `network-backup.functions.ts` → `backupMyNetwork()` — استعلامات مباشرة
- [x] `network-restore.functions.ts` → `restoreMyNetwork()` — نفس منطق إعادة
      الربط والتحقق حرفياً (300+ سطر منطق أعمال، بدون أي تبسيط)
- [x] `agent-backup.functions.ts` → `backupMyAgentData()` / `restoreMyAgentData()`
- [x] `admin-agents.functions.ts` → `adminUpdateAgent()` / `adminDeleteAgent()`
      — مربوطة فعلياً بشاشة المناديب (قائمة تعديل/حذف)
- [x] `superadmin-agents.functions.ts` → `superadminUpdateUserPhone()`
- [x] الإعدادات: بطاقة نسخ احتياطي/استعادة (مدير ومندوب) + منطقة الخطر
      (مسح البيانات) — مربوطتان فعلياً بشاشة الإعدادات
- [ ] `superadmin-backup.functions.ts` (113 سطر) — نسخ احتياطي شامل لكل
      الشبكات (سوبر أدمن) — نفس النمط بالضبط، مؤجَّل فقط لضيق الوقت، مو لسبب
      تقني، يُبنى بنفس الأسلوب أي وقت.

**الخلاصة**: بعد اكتمال المشروع، **Render يمكن إيقافه بالكامل** — التطبيق
الجديد يتواصل فقط مع Supabase مباشرة (نفس قاعدة البيانات، صفر تغيير) وFirebase
(للإشعارات) والميكروتك (اتصال مباشر). هذا تبسيط معماري كبير عن النسخة الأصلية.

## المرحلة 9 — البناء والنشر النهائي
👉 **راجع `PHASE9_BUILD_GUIDE.md`** — دليل شامل: إعداد Android/Firebase،
قائمة اختبار كل ميزة من المراحل 1-8، وقرار الاستبدال مقابل التشغيل الموازي.
- [ ] توليد مشروع Android فعلي (`flutter create`) — عندك
- [ ] ربط Firebase (نفس `google-services.json`) — عندك
- [ ] بناء APK واختبار شامل — عندك، حسب القائمة بالدليل
- [ ] القرار: تشغيل موازٍ أولاً (موصى به)، ثم الاستبدال لاحقاً

---

## قاعدة العمل بكل مرحلة

1. أبني الكود وأشرح وش تغيّر
2. تجرّبه عندك (بما إني ما أقدر أشغّل Flutter من عندي)
3. ترسل لي أي خطأ أو ملاحظة
4. بعد التأكيد، ننتقل للمرحلة التالية

**لا تُحذف أو تُعدَّل أي جدول أو RPC بقاعدة البيانات في أي مرحلة** — التحويل
كله بطرف العميل (Flutter) فقط، إلا المرحلة 8 اللي فيها قرار مصير Render.
