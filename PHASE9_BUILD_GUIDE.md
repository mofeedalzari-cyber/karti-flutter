# المرحلة 9 — البناء والنشر النهائي

## ⚡ الطريقة الأسرع: أمر واحد يسوي كل شي

جهّزت سكربت أتمتة (`build_flutter_apk.mjs`) يسوي كل الخطوات تلقائياً: إنشاء
المشروع، نسخ الملفات، تثبيت الحزم، الأذونات، ربط Firebase، والبناء.

**الشرط الوحيد**: يجب أن يكون **Flutter SDK مثبّتاً عندك مسبقاً** — تأكد
بكتابة `flutter --version` بالطرفية. لو ما عندك، حمّله من
`docs.flutter.dev/get-started/install` أولاً (هذا الجزء الوحيد اللي ما أقدر
أوتمته، لأنه تثبيت أداة على جهازك مباشرة).

بعد التأكد من Flutter، من داخل مجلد هذا الأرشيف (اللي فيه `build_flutter_apk.mjs`)
شغّل:

```bash
node build_flutter_apk.mjs
```

وخلاص — بينشئ المشروع، يجهّزه بالكامل، ويبني `app-release.apk` بنهاية
التشغيل. لو طلعت أي رسالة خطأ أثناء التشغيل، انسخها لي وأحلها.

---

## أو يدوياً خطوة بخطوة (لو تفضّل التحكم الكامل)

## 1) إنشاء المشروع الفعلي

```bash
flutter create --org com.mofeed --project-name karti karti_app
cd karti_app
```

انسخ **كل** محتوى مجلد `lib/` وملف `pubspec.yaml` من آخر أرشيف استلمته فوق
المشروع الجديد (استبدال كامل)، ثم:

```bash
flutter pub get
```

---

## 2) إعداد `AndroidManifest.xml`

افتح `android/app/src/main/AndroidManifest.xml` وتأكد من وجود هذي الأذونات
(أضِف الناقص داخل وسم `<manifest>`، قبل `<application>`):

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="android.permission.READ_CONTACTS" />
<uses-permission android:name="android.permission.WRITE_CONTACTS" />
```

هذي **نفس** الأذونات المستخدمة بتطبيقك الحالي (`com.mofeed.karti`) بالضبط —
تحقق منها بأمر `adb shell dumpsys package com.mofeed.karti | grep permission`
لو حاب تقارن.

### فتح واتساب/الروابط الخارجية (Android 11+)

أضف هذا داخل `<queries>` (أنشئه إذا مو موجود، كعنصر شقيق لـ `<application>`
داخل `<manifest>`):

```xml
<queries>
  <intent>
    <action android:name="android.intent.action.VIEW" />
    <data android:scheme="https" />
  </intent>
</queries>
```

---

## 3) ربط Firebase

بما إن معرّف الحزمة **نفسه بالضبط** (`com.mofeed.karti`)، أعد استخدام **نفس**
ملف `google-services.json` الحالي مباشرة — لا تحتاج تشغّل `flutterfire configure`
ولا تنشئ تطبيق Firebase جديد:

```bash
# انسخ google-services.json الموجود عندك من المشروع القديم إلى:
android/app/google-services.json
```

أضف بملف `android/build.gradle` (مستوى المشروع):
```gradle
buildscript {
  dependencies {
    classpath 'com.google.gms:google-services:4.4.2'
  }
}
```

وبنهاية `android/app/build.gradle`:
```gradle
apply plugin: 'com.google.gms.google-services'
```

(نفس الخطوتين بالضبط اللي سويناهن للنسخة السابقة عبر `setup-firebase-android.mjs`
— هنا نسويها يدوياً لأن مشروع Flutter مختلف).

---

## 4) تأكد من `minSdkVersion`

بملف `android/app/build.gradle`، تأكد إن `minSdkVersion` **24 على الأقل**
(نفس القيمة المستخدمة بتطبيقك الحالي — بعض المكتبات المستخدمة هنا، خصوصاً
`firebase_messaging` و`syncfusion_flutter_pdf`، تتطلب هذا الحد الأدنى):

```gradle
defaultConfig {
    applicationId "com.mofeed.karti"
    minSdkVersion 24
    // ...
}
```

---

## 5) التشغيل والبناء

```bash
# للتجربة السريعة على جهاز/محاكي متصل
flutter run

# لبناء APK فعلي
flutter build apk --release
```

الملف الناتج: `build/app/outputs/flutter-apk/app-release.apk`

---

## 6) قائمة اختبار شاملة (لا تتجاوزها!)

اختبر **كل بند** قبل ما تقرر استبدال التطبيق الحالي. علّم كل واحد لما يشتغل:

### أساسي
- [ ] تسجيل الدخول (مندوب) و(مدير) — رقم جوال + كلمة مرور
- [ ] إنشاء حساب جديد (مندوب وشبكة) من شاشة التسجيل
- [ ] لوحة التحكم تعرض بيانات صحيحة (مختلفة حسب الدور)

### المخزون
- [ ] عرض/إضافة/تعديل/حذف شبكة
- [ ] عرض/إضافة/تعديل/حذف باقة، وطلب سحب، وبيع مباشر
- [ ] رفع كروت (نص، وملف TXT/CSV/JSON، **و PDF** لو فعّلت ترخيص Syncfusion)
- [ ] إدارة الكروت: فلاتر، حذف/إرجاع، نقل مباع، طباعة PDF

### العمليات
- [ ] قائمة المناديب: بحث، تفعيل/تعطيل، **تعديل**، **حذف نهائي**
- [ ] طلبات السحب: اعتماد، رفض، تسجيل دفعة (**تحقق من طباعة الإيصال تلقائياً**)
- [ ] طلبات الانضمام: قبول ورفض
- [ ] حسابات المناديب: الجداول والتسوية

### المبيعات والعملاء
- [ ] كبينة البيع: اختيار زبون (**جرّب زر جهات الاتصال**)، بيع، مشاركة الفاتورة
- [ ] الزبائن: تسديد، شحن يدوي، **طباعة فاتورة PDF**
- [ ] المبيعات: فلاتر، تعديل، حذف
- [ ] المدفوعات: تسديد دين مندوب + طباعة الإيصال

### الإدارة
- [ ] الإعدادات: تعديل الملف الشخصي
- [ ] **النسخ الاحتياطي**: صدّر نسخة، تأكد الملف يوصل عبر المشاركة
- [ ] **الاستعادة**: جرّبها بحساب/شبكة تجريبية أولاً — **لا تجربها على بيانات حقيقية أول مرة**
- [ ] **منطقة الخطر (مسح البيانات)**: لا تختبرها إلا بحساب/قاعدة بيانات تجريبية منفصلة تماماً!
- [ ] سجل النشاط
- [ ] لوحة السوبر أدمن
- [ ] استعادة كلمة المرور

### الميكروتك ⚠️ (أعلى مخاطرة، اختبرها بعناية)
- [ ] إضافة جهاز + اختبار الاتصال
- [ ] نظرة عامة، النشطون، الكروت، الباقات

### الإشعارات
- [ ] وصول إشعار فعلي (اعتماد طلب مثلاً) والتطبيق مغلق تماماً
- [ ] الضغط على الإشعار يفتح الشاشة الصحيحة

### التقارير
- [ ] تصدير PDF من لوحة التحكم
- [ ] تصدير Excel من لوحة التحكم

---

## 7) القرار النهائي: استبدال أم تشغيل موازٍ؟

**توصيتي**: لا تستبدل التطبيق الحالي فوراً. ثبّت نسخة Flutter **بجانب**
التطبيق الحالي (Capacitor) على جهازك الشخصي فقط لمدة أسبوع أو أسبوعين، استخدمها
بعملك اليومي الفعلي، وتأكد ما فيه نقص أو خطأ قبل ما توزّعها على باقي المناديب.

بما إن قاعدة البيانات (Supabase) **نفسها بالضبط** بين النسختين، تقدر تستخدم
التطبيقين بالتبادل بأمان تام أثناء فترة التجربة — أي تعديل بأي نسخة ينعكس فوراً
بالثانية.

---

## ملخص كل المشروع

| # | المرحلة | الحالة |
|---|---|---|
| 1-8 | البناء (كل الصفحات والميزات) | ✅ 100% |
| 9 | البناء والاختبار الفعلي | 👉 عندك الآن |

بعد ما تخلص الاختبار، أرسل لي أي خطأ يطلع (حتى الصغير) وأصلحه فوراً — تماماً
بنفس الأسلوب اللي مشينا عليه بكل هالمحادثة من أولها.
